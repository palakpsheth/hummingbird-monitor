# ---- secrets ----
.env
.env.*
!.env.example

# ---- VCS ----
# Note: .git is intentionally NOT excluded so the Dockerfile can extract
# the current commit hash during build for display in the web UI footer.
# Only minimal metadata (HEAD, refs, packed-refs) is copied into the image.
.gitignore

# ---- python caches / venv ----
__pycache__
*.py[cod]
*.pyd
*.so
*.egg-info
.eggs
.venv
.uv
uv.lock
build
dist

# ---- tests / docs you may not need in image ----
tests
docs
*.md
!README.md

# ---- editor / OS ----
.vscode
.idea
.DS_Store
Thumbs.db

# ---- local runtime data ----
data
media
exports
*.sqlite
*.sqlite3

# ---- large archives ----
*.tar.gz
*.zip
